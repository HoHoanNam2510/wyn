# migrate

Create and apply a new Prisma migration. Prompts for a migration name.

```bash
npx prisma migrate dev
```

After schema changes, always run `npx prisma generate` to update the Prisma Client types.
